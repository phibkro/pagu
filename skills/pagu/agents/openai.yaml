interface:
  display_name: "Pagu Access"
  short_description: "Request exact read access through pagu"
  default_prompt: "Use $pagu to request the exact read path needed after this sandbox denial."

policy:
  allow_implicit_invocation: true
