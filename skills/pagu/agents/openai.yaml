interface:
  display_name: "Pagu"
  short_description: "Run agents through Pagu with clear role boundaries"
  default_prompt: "Use $pagu to start or operate this agent journey through the appropriate Pagu role and profile."

policy:
  allow_implicit_invocation: true
